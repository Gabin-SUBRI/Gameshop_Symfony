<!DOCTYPE html>
<html>
<head>
    <title>Connexion - GameShop</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-blue-900 to-blue-500 min-h-screen flex items-center justify-center">

    <div class="bg-blue-800 rounded-2xl p-8 w-full max-w-md shadow-2xl border border-blue-700">
        <h1 class="text-4xl font-black text-white text-center mb-2">🎮 GameShop</h1>
        <p class="text-blue-300 text-center mb-8 uppercase tracking-widest text-sm">Connexion</p>

        {% if error %}
    <div class="bg-red-500 text-white rounded-lg p-3 mb-6 text-center font-bold">
        Email ou mot de passe incorrect.
    </div>
{% endif %}

        <form method="post">
            <div class="mb-4">
                <label class="text-blue-200 font-bold block mb-2">Email</label>
                <input type="email" name="_username" value="{{ last_username }}"
                    class="w-full bg-blue-900 text-white rounded-lg px-4 py-3 border border-blue-600 focus:outline-none focus:border-white"
                    placeholder="votre@email.com" autocomplete="email">
            </div>
            <div class="mb-6">
                <label class="text-blue-200 font-bold block mb-2">Mot de passe</label>
                <input type="password" name="_password"
                    class="w-full bg-blue-900 text-white rounded-lg px-4 py-3 border border-blue-600 focus:outline-none focus:border-white"
                    placeholder="••••••••" autocomplete="current-password">
            </div>
            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
            <button type="submit"
                class="w-full bg-white text-blue-700 font-black py-3 rounded-full hover:bg-blue-100 transition-all text-lg">
                Se connecter →
            </button>
        </form>

        <p class="text-blue-300 text-center mt-6">
            Pas encore de compte ?
            <a href="{{ path('app_register') }}" class="text-white font-bold hover:underline">S'inscrire</a>
        </p>
        <p class="text-center mt-3">
            <a href="{{ path('app_home') }}" class="text-blue-400 hover:underline text-sm">← Retour à l'accueil</a>
        </p>
    </div>

</body>
</html>