<!DOCTYPE html>
<html>
<head>
    <title>Mon Panier</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-blue-500 p-8">
    <h1 class="text-3xl font-bold text-center text-white mb-8">Mon Panier</h1>

    {% if panier is empty %}
        <p class="text-white text-center">Votre panier est vide !</p>
    {% else %}
        {% for index, nom in panier %}
            <div class="bg-blue-700 rounded-lg p-4 text-white flex justify-between items-center mb-4 w-96 mx-auto">
                <p class="font-bold">{{ nom }}</p>
                <a href="{{ path('app_panier_supprimer', {'index': index}) }}">
                    <button class="bg-red-500 text-white px-4 py-2 rounded font-bold hover:bg-red-600">Supprimer</button>
                </a>
            </div>
        {% endfor %}
    {% endif %}

    <div class="text-center mt-8">
        <a href="{{ path('app_jeux') }}">
            <button class="bg-white text-blue-500 px-4 py-2 rounded font-bold hover:bg-blue-100">Retour à la boutique</button>
        </a>
    </div>

</body>
</html>